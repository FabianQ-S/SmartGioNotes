// Top level compilable build.gradle file for an Android project
// This file is used to configure the build settings for the entire project.
// It includes the Android Gradle plugin and Google services plugin.

plugins {
    alias(libs.plugins.android.application) apply false
    id 'com.google.gms.google-services' version '4.4.0' apply false
}