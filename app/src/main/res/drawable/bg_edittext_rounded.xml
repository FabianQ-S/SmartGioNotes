<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Color de fondo -->
    <solid android:color="#FFFFFF" />

    <!-- Borde gris claro -->
    <stroke
        android:width="1dp"
        android:color="#DDDDDD" />

    <!-- Bordes redondeados -->
    <corners android:radius="12dp" />

    <!-- Espaciado interno (opcional si no usas padding en el EditText) -->
    <padding
        android:left="10dp"
        android:top="8dp"
        android:right="10dp"
        android:bottom="8dp" />
</shape>